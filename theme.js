import { base } from "@theme-ui/presets";

const theme = {
  ...base,
  colors: {
    text: "#c9d8db",
    background: "#161d1d",
  },
  styles: {
    body: {
      background: "background",
    },
    h1: {
      color: "text",
    },
  },
};

export default theme;
